import { getClothesById } from '@/app/data/data-utils'
import styles from './Item.module.css'
import React from 'react'

/*
export const Item = (props) => {
    const clothes = getClothesById(props.params.id);
    return (
        <main className={styles.content}>
            {clothes ? (
                <div className={styles.product}>
                    <img
                    alt={clothes.name}
                    src={clothes.image}
                    className={styles.product_image}
                    />
                    <h2 className={styles.product_title}>{clothes.name}</h2>
                    <h3 className={styles.product_price}>{clothes.price}</h3>
                </div>
            )}
        </main>
    )
}*/